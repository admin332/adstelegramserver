

## План: Шаг 2 и 3 онбординга заказа рекламы

### Обзор

Расширяем OrderDrawer для поддержки полного флоу заказа:
- **Шаг 1**: Выбор количества постов (уже готов)
- **Шаг 2**: Выбор даты и времени публикации
- **Шаг 3**: Выбор рекламной кампании (или создание новой)

---

### Шаг 2: Выбор даты и времени

#### Логика по умолчанию:
- Дата: сегодня
- Время: ближайший доступный час (текущий час + 1, если не занят)
- Длительность: 24 часа на каждый пост

#### UI компоненты:
```
┌─────────────────────────────────────────┐
│  ████████████████░░░░░░  Шаг 2 из 3    │
│                                         │
│      Выберите дату и время              │
│      Реклама в канале CryptoNews        │
├─────────────────────────────────────────┤
│                                         │
│  📅 Дата публикации                     │
│  ┌─────────────────────────────────┐   │
│  │  26 января 2026  ▼              │   │ ← Popover с Calendar
│  └─────────────────────────────────┘   │
│                                         │
│  🕐 Время начала                        │
│  ┌─────────────────────────────────┐   │
│  │  14:00  ▼                       │   │ ← Выбор времени
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  Начало: 26 янв 2026, 14:00     │   │ ← Итоговая информация
│  │  Конец:  27 янв 2026, 14:00     │   │   с синей пунктирной
│  │  Длительность: 24 часа          │   │   обводкой
│  └─────────────────────────────────┘   │
│                                         │
│  [          Далее →           ]         │
└─────────────────────────────────────────┘
```

---

### Шаг 3: Выбор рекламной кампании

#### Логика:
- Показать список сохраненных кампаний
- Если количество постов > 1 — мульти-выбор
- Если нет сохраненных — кнопка "Создать кампанию"
- Тестовая кампания с фото, текстом и URL-кнопкой

#### UI компоненты:
```
┌─────────────────────────────────────────┐
│  ████████████████████  Шаг 3 из 3      │
│                                         │
│      Выберите рекламную кампанию        │
│      Выберите 2 кампании                │ ← если quantity > 1
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ ☑️ Крипто Продукт               │   │ ← Тестовая кампания
│  │  [📷 Фото]                      │   │
│  │  Лучший крипто-кошелёк для...   │   │
│  │  🔗 Узнать больше               │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  ➕ Создать новую кампанию      │   │
│  └─────────────────────────────────┘   │
│                                         │
│  [        Оформить заказ        ]       │
└─────────────────────────────────────────┘
```

---

### Новые файлы

#### 1. `src/components/channel/DateTimeSelector.tsx`
Компонент выбора даты и времени:
- Calendar в Popover для выбора даты
- Выпадающий список для выбора времени (часы с 00:00 до 23:00)
- Отображение начала и конца рекламы
- Блок с синей пунктирной обводкой для итога

```typescript
interface DateTimeSelectorProps {
  selectedDate: Date;
  selectedHour: number;
  onDateChange: (date: Date) => void;
  onHourChange: (hour: number) => void;
}
```

#### 2. `src/components/channel/CampaignSelector.tsx`
Компонент выбора кампаний:
- Список сохраненных кампаний с чекбоксами
- Превью: фото, текст, кнопка
- Кнопка "Создать новую кампанию"
- Мульти-выбор если quantity > 1

```typescript
interface Campaign {
  id: string;
  name: string;
  imageUrl: string;
  text: string;
  buttonText: string;
  buttonUrl: string;
}

interface CampaignSelectorProps {
  campaigns: Campaign[];
  selectedCampaigns: string[];
  requiredCount: number;
  onSelectionChange: (ids: string[]) => void;
  onCreateNew: () => void;
}
```

#### 3. `src/data/mockCampaigns.ts`
Тестовые данные кампаний:
```typescript
export const mockCampaigns: Campaign[] = [
  {
    id: "1",
    name: "Крипто Кошелёк Pro",
    imageUrl: "https://images.unsplash.com/photo-1621761191319-c6fb62004040?w=400",
    text: "Безопасный криптокошелёк с поддержкой 100+ монет. Храни и обменивай крипту без комиссий!",
    buttonText: "Скачать бесплатно",
    buttonUrl: "https://example.com/wallet",
  }
];
```

---

### Изменения в существующих файлах

#### `src/components/channel/OrderDrawer.tsx`

**Добавить состояния:**
```typescript
const [selectedDate, setSelectedDate] = useState(new Date());
const [selectedHour, setSelectedHour] = useState(() => {
  const nextHour = new Date().getHours() + 1;
  return nextHour > 23 ? 0 : nextHour;
});
const [selectedCampaigns, setSelectedCampaigns] = useState<string[]>([]);
```

**Обновить handleNext:**
```typescript
const handleNext = () => {
  if (currentStep < totalSteps) {
    setCurrentStep(currentStep + 1);
  } else {
    // Финальный шаг — оформление заказа
    console.log('Order submitted', { 
      quantity, selectedDate, selectedHour, selectedCampaigns 
    });
    onClose();
  }
};

const handleBack = () => {
  if (currentStep > 1) {
    setCurrentStep(currentStep - 1);
  }
};
```

**Обновить заголовки по шагам:**
```typescript
const stepTitles = {
  1: 'Выберите количество постов',
  2: 'Выберите дату и время',
  3: 'Выберите рекламную кампанию',
};
```

**Условный рендеринг контента:**
```tsx
{currentStep === 1 && <PostQuantitySelector ... />}
{currentStep === 2 && <DateTimeSelector ... />}
{currentStep === 3 && <CampaignSelector ... />}
```

**Кнопка "Назад" в footer:**
```tsx
<DrawerFooter className="flex-row gap-2">
  {currentStep > 1 && (
    <Button variant="ghost" onClick={handleBack}>
      Назад
    </Button>
  )}
  <Button onClick={handleNext} className="flex-1">
    {currentStep === totalSteps ? 'Оформить заказ' : 'Далее'}
  </Button>
</DrawerFooter>
```

---

### Обновление Calendar для работы в Drawer

В `src/components/ui/calendar.tsx` добавить `pointer-events-auto`:
```tsx
className={cn("p-3 pointer-events-auto", className)}
```

---

### Порядок реализации

1. Создать `src/data/mockCampaigns.ts` — тестовые данные кампаний
2. Создать `src/components/channel/DateTimeSelector.tsx` — выбор даты/времени
3. Создать `src/components/channel/CampaignSelector.tsx` — выбор кампаний
4. Обновить `src/components/ui/calendar.tsx` — добавить pointer-events-auto
5. Обновить `src/components/channel/OrderDrawer.tsx` — интегрировать все шаги

---

### Техническая сводка

| Файл | Действие |
|------|----------|
| `src/data/mockCampaigns.ts` | Создать |
| `src/components/channel/DateTimeSelector.tsx` | Создать |
| `src/components/channel/CampaignSelector.tsx` | Создать |
| `src/components/ui/calendar.tsx` | Изменить (pointer-events-auto) |
| `src/components/channel/OrderDrawer.tsx` | Изменить (добавить шаги 2 и 3) |

