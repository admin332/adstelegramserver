
## Цель
Добавить отображение сделок для владельцев каналов в разделе "Мои сделки" с возможностью одобрить, отклонить или предложить изменения.

## Текущее состояние

### Проблема
Хук `useUserDeals` (строка 53) фильтрует только по `advertiser_id`:
```typescript
.eq('advertiser_id', user.id)
```
Это означает, что владельцы каналов не видят заказы на свои каналы.

### RLS политики для deals
- `Users can view own deals` — только `advertiser_id = auth.uid()`
- Нет политики для владельцев каналов

### Данные
Сделки связаны с каналами через `channel_id`. Владелец канала определяется через `channels.owner_id` или `channel_admins`.

## Решение

### Архитектура
Создать Edge Function `user-deals` для безопасного получения сделок с учетом обеих ролей:
- **Как рекламодатель**: сделки где `advertiser_id = user.id`
- **Как владелец канала**: сделки на каналы пользователя (через `channel_admins`)

Создать Edge Function `deal-action` для обработки действий владельца:
- Одобрить (`approve`)
- Отклонить (`reject`)
- Предложить изменения (открывает чат в боте)

### 1. Создать Edge Function `user-deals`

```text
supabase/functions/user-deals/index.ts
```

Логика:
1. Валидировать Telegram `initData`
2. Найти user_id по telegram_id
3. Получить ID каналов пользователя из `channel_admins`
4. Запросить сделки:
   - WHERE `advertiser_id = user.id` (как рекламодатель)
   - OR `channel_id IN (каналы пользователя)` (как владелец)
5. Добавить поле `role` к каждой сделке (`advertiser` | `channel_owner`)
6. Добавить данные о рекламодателе для сделок как владелец

### 2. Создать Edge Function `deal-action`

```text
supabase/functions/deal-action/index.ts
```

Принимает:
- `dealId` — ID сделки
- `action` — `approve` | `reject` | `request_changes`
- `initData` — для валидации

Действия:
- **approve**: Обновить статус на `in_progress`, уведомить рекламодателя
- **reject**: Обновить статус на `cancelled`, вернуть средства, уведомить рекламодателя
- **request_changes**: Открыть диалог в боте для обсуждения

### 3. Обновить хук `useUserDeals`

Переключить на использование Edge Function вместо прямого запроса к Supabase:

```typescript
export interface Deal {
  id: string;
  status: DealStatus;
  // ... существующие поля
  role: 'advertiser' | 'channel_owner';  // Новое поле
  advertiser?: {                          // Новое: данные рекламодателя
    first_name: string;
    username?: string;
    photo_url?: string;
  };
}

export function useUserDeals() {
  return useQuery({
    queryKey: ['user-deals'],
    queryFn: async () => {
      const initData = getTelegramInitData();
      if (!initData) return [];

      const response = await fetch(
        `${SUPABASE_URL}/functions/v1/user-deals`,
        {
          method: "POST",
          headers: { "Content-Type": "application/json", "apikey": ANON_KEY },
          body: JSON.stringify({ initData }),
        }
      );
      
      const data = await response.json();
      if (!data.success) throw new Error(data.error);
      return data.deals;
    },
  });
}
```

### 4. Обновить `DealCard`

Добавить поддержку роли `channel_owner`:
- Показывать информацию о рекламодателе вместо канала
- Добавить кнопки "Одобрить", "Отклонить", "Изменения" для статуса `escrow`

```typescript
interface DealCardProps {
  // ... существующие
  role: 'advertiser' | 'channel_owner';
  advertiser?: {
    first_name: string;
    username?: string;
    photo_url?: string;
  };
  onApprove?: () => void;
  onReject?: () => void;
  onRequestChanges?: () => void;
}
```

### 5. Обновить страницу `Deals.tsx`

Добавить:
- Фильтр по роли (Мои заказы / Заказы на мои каналы)
- Обработчики для действий владельца
- Диалог подтверждения для отклонения

### 6. Создать компонент `OwnerActionsDialog`

```text
src/components/deals/OwnerActionsDialog.tsx
```

Диалог для владельца канала с кнопками:
- ✅ Одобрить — подтвердить публикацию
- ❌ Отклонить — отменить сделку с возвратом средств
- ✏️ Предложить изменения — открыть чат с рекламодателем

## Изменения в базе данных

Не требуются — используем Edge Functions для обхода RLS.

## Файлы для создания/изменения

| Файл | Действие |
|------|----------|
| `supabase/functions/user-deals/index.ts` | Создать |
| `supabase/functions/deal-action/index.ts` | Создать |
| `supabase/config.toml` | Добавить функции |
| `src/hooks/useUserDeals.ts` | Обновить на Edge Function |
| `src/components/DealCard.tsx` | Добавить поддержку роли владельца |
| `src/components/deals/OwnerActionsDialog.tsx` | Создать |
| `src/pages/Deals.tsx` | Добавить фильтр и действия |

## Структура данных

### Ответ user-deals
```typescript
{
  success: true,
  deals: [
    {
      id: "...",
      status: "escrow",
      total_price: 10,
      posts_count: 1,
      duration_hours: 24,
      escrow_address: "EQ...",
      scheduled_at: "2026-01-27T00:00:00Z",
      created_at: "2026-01-26T19:00:00Z",
      expires_at: null,
      role: "channel_owner",  // <-- Роль пользователя
      channel: { id, title, avatar_url, username },
      campaign: { id, name },
      advertiser: {           // <-- Данные рекламодателя (для владельца)
        first_name: "Andrey",
        username: "romanov_design",
        photo_url: "..."
      }
    }
  ]
}
```

### UI для владельца канала

```text
┌─────────────────────────────────────┐
│ [Avatar] Andrey @romanov_design     │  <- Рекламодатель
│          Кампания: Лето 2026        │
├─────────────────────────────────────┤
│ 💎 10 TON ≈ $35   • 1 пост • 24ч   │
├─────────────────────────────────────┤
│ 🛡 Оплачено              2 мин назад│
├─────────────────────────────────────┤
│ [✅ Одобрить] [❌ Отклонить] [✏️]   │
└─────────────────────────────────────┘
```

## Telegram уведомления

При действии владельца отправляем уведомление рекламодателю:

**Одобрено:**
```
✅ Ваша реклама одобрена!

Канал: Channel Name
Публикация: 27.01.2026 в 03:00

Ожидайте размещения.
```

**Отклонено:**
```
❌ Реклама отклонена

Канал: Channel Name
Средства будут возвращены на ваш кошелёк.
```

## Результат

После реализации:
1. Владельцы каналов видят заказы на свои каналы в "Мои сделки"
2. Сделки разделены по ролям (фильтр "Мои заказы" / "Заказы на каналы")
3. Владелец может одобрить, отклонить или запросить изменения
4. Рекламодатель получает уведомления о решении владельца
