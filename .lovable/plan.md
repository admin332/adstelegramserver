
## План: Полнофункциональная страница /create с добавлением каналов и кампаний

### Обзор

Расширяем страницу `/create` для поддержки полного флоу после выбора роли:
- **Рекламодатель**: Форма создания рекламной кампании
- **Владелец канала**: Процесс добавления и верификации канала через Telegram Bot API

---

### Архитектура решения

```text
┌─────────────────────────────────────────────────────────────┐
│                    /create page                              │
├─────────────────────────────────────────────────────────────┤
│  Step 1: Role Selection (existing)                           │
│    ├── Рекламодатель                                        │
│    └── Владелец канала                                      │
├─────────────────────────────────────────────────────────────┤
│  Step 2: Role-specific flow                                  │
│    ├── Advertiser: Campaign Creation Form                   │
│    └── Channel Owner: Channel Addition Wizard               │
└─────────────────────────────────────────────────────────────┘
```

---

### Часть 1: База данных

#### 1.1 Таблица `channels` для хранения каналов

```sql
CREATE TABLE public.channels (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  owner_id uuid REFERENCES public.users(id) ON DELETE CASCADE NOT NULL,
  telegram_chat_id bigint UNIQUE,
  username text NOT NULL,
  title text,
  description text,
  avatar_url text,
  subscribers_count integer DEFAULT 0,
  category text NOT NULL,
  -- Pricing
  price_1_24 numeric(10,2),  -- 1 пост / 24 часа
  price_2_48 numeric(10,2),  -- 2 поста / 48 часов
  price_post numeric(10,2),  -- Обычный пост
  -- Status
  verified boolean DEFAULT false,
  bot_is_admin boolean DEFAULT false,
  is_active boolean DEFAULT true,
  -- Timestamps
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);
```

#### 1.2 Таблица `campaigns` для рекламных кампаний

```sql
CREATE TABLE public.campaigns (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  owner_id uuid REFERENCES public.users(id) ON DELETE CASCADE NOT NULL,
  name text NOT NULL,
  image_url text,
  text text NOT NULL,
  button_text text,
  button_url text,
  is_active boolean DEFAULT true,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);
```

#### 1.3 RLS политики

```sql
-- Channels: Владелец может CRUD свои каналы
ALTER TABLE public.channels ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view all active channels"
  ON public.channels FOR SELECT
  USING (is_active = true);

CREATE POLICY "Users can manage own channels"
  ON public.channels FOR ALL
  USING (owner_id IN (SELECT id FROM public.users WHERE telegram_id = (auth.jwt()->>'telegram_id')::bigint));

-- Campaigns: Владелец может CRUD свои кампании  
ALTER TABLE public.campaigns ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own campaigns"
  ON public.campaigns FOR SELECT
  USING (owner_id IN (SELECT id FROM public.users WHERE telegram_id = (auth.jwt()->>'telegram_id')::bigint));

CREATE POLICY "Users can manage own campaigns"
  ON public.campaigns FOR ALL
  USING (owner_id IN (SELECT id FROM public.users WHERE telegram_id = (auth.jwt()->>'telegram_id')::bigint));
```

---

### Часть 2: Edge Function для верификации канала

#### 2.1 `supabase/functions/verify-channel/index.ts`

Эта функция будет:
1. Получать username канала от пользователя
2. Вызывать Telegram Bot API `getChat` для получения информации о канале
3. Вызывать `getChatMember` для проверки:
   - Является ли бот @adsingo_bot администратором
   - Является ли текущий пользователь создателем/администратором
4. Если всё ОК - сохранять канал в БД с `verified: true`

```typescript
// Основная логика верификации
async function verifyChannel(username: string, userId: number, botToken: string) {
  const chatId = username.startsWith('@') ? username : `@${username}`;
  
  // 1. Получить информацию о канале
  const chatResponse = await fetch(
    `https://api.telegram.org/bot${botToken}/getChat?chat_id=${chatId}`
  );
  const chatData = await chatResponse.json();
  
  if (!chatData.ok) {
    return { success: false, error: 'Канал не найден' };
  }
  
  const chat = chatData.result;
  
  // 2. Проверить что бот - админ с правом публикации
  const botId = /* получить из токена */;
  const botMemberResponse = await fetch(
    `https://api.telegram.org/bot${botToken}/getChatMember?chat_id=${chat.id}&user_id=${botId}`
  );
  const botMember = await botMemberResponse.json();
  
  const botCanPost = botMember.ok && 
    botMember.result.status === 'administrator' && 
    botMember.result.can_post_messages;
  
  // 3. Проверить что пользователь - админ/создатель
  const userMemberResponse = await fetch(
    `https://api.telegram.org/bot${botToken}/getChatMember?chat_id=${chat.id}&user_id=${userId}`
  );
  const userMember = await userMemberResponse.json();
  
  const userIsAdmin = userMember.ok && 
    ['creator', 'administrator'].includes(userMember.result.status);
  
  return {
    success: botCanPost && userIsAdmin,
    chat: {
      id: chat.id,
      title: chat.title,
      username: chat.username,
      description: chat.description,
      photo: chat.photo,
      member_count: /* получить через getChatMemberCount */
    },
    botCanPost,
    userIsAdmin
  };
}
```

---

### Часть 3: Frontend компоненты

#### 3.1 Обновленная структура `/create`

```text
src/pages/Create.tsx
  └── Шаг 1: RoleSelector (существующий код)
  └── Шаг 2: 
      ├── CreateCampaignForm (если advertiser)
      └── AddChannelWizard (если channel_owner)
```

#### 3.2 Новые компоненты

##### `src/components/create/AddChannelWizard.tsx`

Многошаговый визард добавления канала:

```text
┌─────────────────────────────────────────┐
│  ████░░░░░░░░░░░░░░░░  Шаг 1 из 3      │
│                                         │
│      Добавьте бота в канал             │
├─────────────────────────────────────────┤
│                                         │
│  📋 Инструкция:                         │
│                                         │
│  1. Откройте настройки канала          │
│  2. Добавьте @adsingo_bot как админа   │
│  3. Выдайте право "Публикация"         │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  [Открыть Telegram]  →          │   │ ← Deep link
│  └─────────────────────────────────┘   │
│                                         │
│  [          Готово, далее →       ]     │
└─────────────────────────────────────────┘
```

```text
┌─────────────────────────────────────────┐
│  ████████░░░░░░░░░░░░  Шаг 2 из 3      │
│                                         │
│      Введите данные канала             │
├─────────────────────────────────────────┤
│                                         │
│  📝 Username канала                     │
│  ┌─────────────────────────────────┐   │
│  │  @                              │   │
│  └─────────────────────────────────┘   │
│                                         │
│  📂 Категория                           │
│  ┌─────────────────────────────────┐   │
│  │  Криптовалюты  ▼                │   │
│  └─────────────────────────────────┘   │
│                                         │
│  💰 Стоимость размещения               │
│  ┌───────────┐ ┌───────────┐          │
│  │ 1/24: $   │ │ 2/48: $   │          │
│  └───────────┘ └───────────┘          │
│                                         │
│  [        Проверить канал →       ]     │
└─────────────────────────────────────────┘
```

```text
┌─────────────────────────────────────────┐
│  ████████████████████  Шаг 3 из 3      │
│                                         │
│      Канал успешно добавлен!           │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  [Avatar]  CryptoNews           │   │
│  │            @cryptonews          │   │
│  │            ✓ Верифицирован      │   │
│  │            125,000 подписчиков  │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ✅ Бот добавлен как администратор     │
│  ✅ Вы подтверждены как владелец       │
│  ✅ Статистика загружена               │
│                                         │
│  [      Перейти к моим каналам    ]     │
└─────────────────────────────────────────┘
```

##### `src/components/create/CreateCampaignForm.tsx`

Форма создания рекламной кампании:

```text
┌─────────────────────────────────────────┐
│                                         │
│      Создание рекламной кампании        │
├─────────────────────────────────────────┤
│                                         │
│  📷 Изображение                         │
│  ┌─────────────────────────────────┐   │
│  │        [+ Загрузить фото]       │   │ ← File upload
│  └─────────────────────────────────┘   │
│                                         │
│  ✏️ Название кампании                   │
│  ┌─────────────────────────────────┐   │
│  │  Крипто Кошелёк Pro             │   │
│  └─────────────────────────────────┘   │
│                                         │
│  📝 Текст рекламы                       │
│  ┌─────────────────────────────────┐   │
│  │  Безопасный криптокошелёк...    │   │
│  │                                 │   │
│  │                                 │   │
│  └─────────────────────────────────┘   │
│                                         │
│  🔗 Кнопка                              │
│  ┌────────────┐ ┌──────────────────┐   │
│  │ Текст кнопки│ │ URL             │   │
│  └────────────┘ └──────────────────┘   │
│                                         │
│  ── Предпросмотр ──────────────────    │
│  ┌─────────────────────────────────┐   │
│  │  [Фото]                         │   │
│  │  Безопасный криптокошелёк...    │   │
│  │  [🔗 Скачать бесплатно]         │   │
│  └─────────────────────────────────┘   │
│                                         │
│  [        Создать кампанию        ]     │
└─────────────────────────────────────────┘
```

---

### Часть 4: Категории каналов

```typescript
// src/data/channelCategories.ts
export const channelCategories = [
  { id: 'crypto', name: 'Криптовалюты', icon: 'Bitcoin' },
  { id: 'tech', name: 'Технологии', icon: 'Cpu' },
  { id: 'marketing', name: 'Маркетинг', icon: 'TrendingUp' },
  { id: 'business', name: 'Бизнес', icon: 'Briefcase' },
  { id: 'games', name: 'Игры', icon: 'Gamepad2' },
  { id: 'lifestyle', name: 'Лайфстайл', icon: 'Heart' },
  { id: 'news', name: 'Новости', icon: 'Newspaper' },
  { id: 'entertainment', name: 'Развлечения', icon: 'Film' },
  { id: 'education', name: 'Образование', icon: 'GraduationCap' },
  { id: 'other', name: 'Другое', icon: 'MoreHorizontal' },
];
```

---

### Порядок реализации

| Шаг | Файл | Описание |
|-----|------|----------|
| 1 | Migration | Создать таблицы `channels` и `campaigns` с RLS |
| 2 | `supabase/functions/verify-channel/index.ts` | Edge function для верификации канала через Telegram Bot API |
| 3 | `src/data/channelCategories.ts` | Справочник категорий каналов |
| 4 | `src/components/create/AddChannelWizard.tsx` | 3-шаговый визард добавления канала |
| 5 | `src/components/create/CreateCampaignForm.tsx` | Форма создания рекламной кампании |
| 6 | `src/pages/Create.tsx` | Интеграция компонентов, логика переключения шагов |
| 7 | `supabase/config.toml` | Настройка verify_jwt = false для verify-channel |

---

### Техническая сводка

| Файл | Действие |
|------|----------|
| **Database Migration** | Создать таблицы channels, campaigns + RLS |
| `supabase/functions/verify-channel/index.ts` | Создать |
| `supabase/config.toml` | Изменить (добавить verify_jwt = false) |
| `src/data/channelCategories.ts` | Создать |
| `src/components/create/AddChannelWizard.tsx` | Создать |
| `src/components/create/CreateCampaignForm.tsx` | Создать |
| `src/pages/Create.tsx` | Изменить (добавить логику шагов) |

---

### Особенности безопасности

1. **Верификация владения каналом** - проверка через Telegram Bot API что пользователь является администратором/создателем канала

2. **Проверка бота** - убедиться что @adsingo_bot добавлен как админ с правом публикации

3. **RLS политики** - пользователи могут управлять только своими каналами и кампаниями

4. **Валидация на сервере** - вся критическая логика выполняется в edge function, а не на клиенте

5. **PR Manager Flow** - поддержка добавления канала не только владельцем, но и менеджером с правами администратора в Telegram
