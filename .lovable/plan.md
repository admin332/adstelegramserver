

## План: Разделить создание кампании на 3 шага с прогресс-баром

### Текущая ситуация

Форма создания кампании (`CreateCampaignForm`) показывает все поля на одном экране. Нужно разбить на 3 шага:

1. **Название кампании** — поле для ввода названия
2. **Изображение или видео** — загрузка медиа без предпросмотра
3. **Текст рекламы и кнопка** — текст + опциональная кнопка

### Структура шагов

```text
Шаг 1: Название кампании
┌─────────────────────────────────┐
│  Прогресс: [████░░░░░░] 33%    │
│                                 │
│  Название кампании *            │
│  ┌─────────────────────────────┐│
│  │ Продвижение приложения      ││
│  └─────────────────────────────┘│
│                                 │
│  [Назад]         [Продолжить]   │
└─────────────────────────────────┘

Шаг 2: Медиа
┌─────────────────────────────────┐
│  Прогресс: [████████░░] 66%    │
│                                 │
│  ┌─────────────────────────────┐│
│  │     📷 Добавить медиа       ││
│  │   Изображение или видео     ││
│  └─────────────────────────────┘│
│  ✓ Файл выбран: image.jpg      │
│                                 │
│  [Назад]         [Продолжить]   │
└─────────────────────────────────┘

Шаг 3: Текст и кнопка
┌─────────────────────────────────┐
│  Прогресс: [██████████] 100%   │
│                                 │
│  Текст рекламы *                │
│  ┌─────────────────────────────┐│
│  │ Напишите текст...           ││
│  └─────────────────────────────┘│
│                                 │
│  Кнопка (опционально)           │
│  [Текст] [https://...]          │
│                                 │
│  [Назад]     [Создать кампанию] │
└─────────────────────────────────┘
```

### Изменения в `src/components/create/CreateCampaignForm.tsx`

#### 1. Добавить состояние шага

```typescript
const [step, setStep] = useState(1);
const progress = (step / 3) * 100;
```

#### 2. Обновить интерфейс CampaignData

Добавить поддержку видео:

```typescript
interface CampaignData {
  name: string;
  text: string;
  button_text: string;
  button_url: string;
}
// + отдельное состояние для файла (уже есть imageFile)
```

#### 3. Изменить handleImageSelect

Поддержка изображений И видео:

```typescript
accept="image/*,video/*"
```

#### 4. Разделить UI на 3 шага

**Шаг 1 — Название:**
- Только поле для ввода названия кампании
- Кнопки: Назад (к выбору роли), Продолжить

**Шаг 2 — Медиа:**
- Область загрузки без предпросмотра изображения
- Показывать только имя файла если выбран
- Кнопка удаления файла
- Кнопки: Назад, Продолжить (можно пропустить)

**Шаг 3 — Текст и кнопка:**
- Textarea для текста рекламы
- Поля для текста кнопки и URL
- Кнопки: Назад, Создать кампанию

#### 5. Валидация по шагам

```typescript
const canProceedStep1 = campaignData.name.trim().length > 0;
const canProceedStep2 = true; // Медиа опционально
const canSubmit = campaignData.text.trim().length > 0;
```

### Техническая структура

```typescript
// Шаг 1
{step === 1 && (
  <div className="space-y-6">
    <ProgressBar value={progress} />
    <NameInput />
    <NavigationButtons 
      onBack={onBack} 
      onNext={() => setStep(2)} 
      canProceed={canProceedStep1}
    />
  </div>
)}

// Шаг 2
{step === 2 && (
  <div className="space-y-6">
    <ProgressBar value={progress} />
    <MediaUpload showFileName={true} showPreview={false} />
    <NavigationButtons 
      onBack={() => setStep(1)} 
      onNext={() => setStep(3)}
    />
  </div>
)}

// Шаг 3
{step === 3 && (
  <div className="space-y-6">
    <ProgressBar value={progress} />
    <AdTextInput />
    <ButtonFields />
    <NavigationButtons 
      onBack={() => setStep(2)} 
      onSubmit={handleSubmit}
      canSubmit={canSubmit}
    />
  </div>
)}
```

### Импорты

Добавить `Progress` из UI компонентов (уже используется в `AddChannelWizard`):

```typescript
import { Progress } from "@/components/ui/progress";
```

### Файл для изменения

| Файл | Изменение |
|------|-----------|
| `src/components/create/CreateCampaignForm.tsx` | Полностью переписать с 3 шагами и прогресс-баром |

### Результат

- Форма разбита на 3 логических шага
- Прогресс-бар показывает текущий шаг (33%, 66%, 100%)
- На шаге медиа нет предпросмотра — только индикатор выбранного файла
- Поддержка видео + изображений
- Навигация между шагами кнопками Назад/Продолжить

