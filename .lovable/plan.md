

# Добавление метки "(МСК)" к времени в уведомлениях

## Проблема

Пользователь выбирает 21:00 в своём часовом поясе, а в уведомлениях видит другое время (22:00) без пояснения, что это московское время.

## Решение

Добавить "(МСК)" к отформатированному времени в функции `formatDate` на бэкенде. Фронтенд менять не нужно — пользователь выбирает удобное ему время, а в уведомлениях просто будет пометка что показано московское время.

## Изменения

### 1. `supabase/functions/check-escrow-payments/index.ts`

Изменить строку 210:

```typescript
// Было:
return `${day}.${month}.${year} в ${hour}:${minute}`;

// Станет:
return `${day}.${month}.${year} в ${hour}:${minute} (МСК)`;
```

### 2. `supabase/functions/notify-deal-payment/index.ts`

Изменить строку 198:

```typescript
// Было:
return `${day}.${month}.${year} в ${hour}:${minute}`;

// Станет:
return `${day}.${month}.${year} в ${hour}:${minute} (МСК)`;
```

## Результат

Сообщения будут выглядеть так:

```
📅 Публикация: 29.01.2026 в 22:00 (МСК)
```

Пользователь понимает, что время указано по Москве.

## Файлы к изменению

| Файл | Изменение |
|------|-----------|
| `supabase/functions/check-escrow-payments/index.ts` | Добавить "(МСК)" в `formatDate` |
| `supabase/functions/notify-deal-payment/index.ts` | Добавить "(МСК)" в `formatDate` |

